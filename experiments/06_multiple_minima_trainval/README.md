# 06 Multiple Minima for TrainVal

Legacy scripts copied from the original project. This folder preserves exploratory code for train-validation smoothness selection and multiple local minima.

Use `outputs/` for regenerated figures/tables. The library implementation lives under `src/trend_estimation/selection/`.
