# 07 Timeweighted Validation Loss

Legacy script copied from the original project. This folder preserves exploratory code for time-weighted validation objectives.

Use `outputs/` for regenerated figures/tables. The library implementation lives under `src/trend_estimation/validation/` and `src/trend_estimation/selection/`.
